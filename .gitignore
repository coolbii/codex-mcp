# dependencies
node_modules/

# build output
dist/

# secrets & local config — NEVER commit
.env
.env.*
!.env.example
*.local
auth.json
*.pem
*.key

# OAuth token store (holds client + refresh tokens)
data/
devspace-oauth.json

# logs
*.log
logs/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# test / coverage
coverage/
.nyc_output/

# Large CJK fonts fetched via `npm run fetch-fonts` (Inter is committed)
assets/fonts/NotoSans*.ttf

# OpenPencil render artifacts
op-preview*.png
